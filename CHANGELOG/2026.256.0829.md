# 2026.256.0829 — docs: correct the variant plugin sets

## Summary

Correct the variant plugin sets in **both** `README.md` and `index.html`. Both
listed the sets as they were before `plugin-mcp`, `plugin-review`, and
`plugin-pipeline` joined the release, and described `charly-full` as "the
default set + udev, preempt".

The `full` variant **omitting `plugin-pipeline` was a product defect**, not a
doc choice: commit `23d69296` added `plugin-pipeline` to `default` only. That
was cured first, in `opencharly/charly#602` (merged), which adds
`plugin-pipeline` to `full` and makes the variant descriptions accurate. These
two docs now match the packaging source (`packaging/charly.yml`, shipped as the
`charly-candy-charly.yml` release asset this repo packages):

- `charly` → secrets, feature, vm, doctor, clean, settings, candy, mcp, review, pipeline (10)
- `charly-full` → secrets, udev, preempt, feature, vm, doctor, clean, settings, candy, mcp, review, pipeline (12)
- `charly-minimal` → doctor, clean, settings (3)

## How tested

The packaging source on charly `main` (post-#602) declares:

```
$ git -C charly show origin/main:packaging/charly.yml | sed -n '/variants:/,/formats:/p'
            variants:
                default:
                    description: The ten everyday welded command plugins.
                    plugins:
                        - plugin-secrets
                        - plugin-feature
                        - plugin-vm
                        - plugin-doctor
                        - plugin-clean
                        - plugin-settings
                        - plugin-candy
                        - plugin-mcp
                        - plugin-review
                        - plugin-pipeline
                full:
                    description: All twelve welded command plugins.
                    plugins:
                        - plugin-secrets
                        - plugin-udev
                        - plugin-preempt
                        - plugin-feature
                        - plugin-vm
                        - plugin-doctor
                        - plugin-clean
                        - plugin-settings
                        - plugin-candy
                        - plugin-mcp
                        - plugin-review
                        - plugin-pipeline
                minimal:
                    description: The doctor/clean/settings core.
                    plugins:
                        - plugin-doctor
                        - plugin-clean
                        - plugin-settings
```

Repo-wide sweep for the removed claims (all four return nothing):

```
$ git grep -n "the default set\|nine everyday\|All eleven\|review (11)" -- .
(none)
```

`index.html` and `README.md` are static hand-authored files; no build or test
exercises their prose. The table markup is unchanged.

## Rulebook compliance

- **R0 — skills first:** `git-workflow` skill loaded.
- **R1 — RCA every anomaly:** the doc↔source divergence was RCA'd to charly commit `23d69296` (pipeline added to `default` only); the fix was routed to the product first.
- **R2 — finish the cutover:** both stale files (`README.md` **and** `index.html`) are corrected; the sweep above confirms no stale variant claim survives in the repo.
- **R3 — no duplication:** the variant sets are not re-derived; the docs point at the single packaging source.
- **R4 — no workarounds:** the underlying product defect was fixed in charly (`#602`), not documented around.
- **R4a — fix the product first:** the docs were updated only after the product cure merged; the earlier blocked revision that encoded the defect as canonical was withdrawn.
- **R5 — delete legacy completely:** the old plugin lists/wording are removed from both files; the sweep pasted above is the claim-keyed self-test.
- **R6 — git safety:** isolated worktree, add-only commits, no force-push, PR-only.
- **R7 — prove behavior, not compilation:** N/A — documentation-only class; no executable behavior changed.
- **R8 — preserve emitted artifacts:** `index.html` is the Pages landing page this repo publishes; its rendered tables are updated in place with unchanged markup.
- **R9 — binary equals source:** N/A — no binary.
- **R10 — fresh disposable proof:** N/A — documentation-only class; no runtime target touched.

## Change classification

- **Change class:** documentation-only (`README.md`, `index.html`).
- **Verification gate:** the packaging source (pasted) + the repo-wide sweep (pasted).
- **Attribution tier:** `documentation reviewed`.

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (documentation reviewed)*