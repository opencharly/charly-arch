# 2026.266.1426 — feat: add the check-arch-repo R10 bed for the published package repo

## Summary of changes

This PR adds the `check-arch-repo` R10 bed to `opencharly/charly-arch` — the repo that OWNS the published pacman artifact the bed proves. The bed was relocated here from `opencharly/charly`'s `charly.yml`; the companion removal is `opencharly/charly#625`.

**Changed files — 2 files, +163 / -0:**

- `charly.yml` (new, +147) — declares the `charly-arch` project (`version: 2026.249.2125`, `repo: github.com/opencharly/charly-arch`, `defaults`) and adds exactly two entities:
  - `arch-repo-vm` — the disposable Arch `cloud_image` VM template: `source.kind: cloud_image`, `distro: arch`, `base_user: arch`, `backend: libvirt`, `disk_size: 20G`, `ram: 4G`, `cpu: 2`, `machine: q35`, `firmware: bios`, `ssh.port_auto: true` + `key_source: generate`, `cloud_init` installing `sudo`.
  - `check-arch-repo` — the R10 bed (`vm.from: arch-repo-vm`, `disposable: true`, `lifecycle: dev`) with an 8-step runtime plan: import + locally sign the charly GPG key, add the `[charly]` repo to `pacman.conf`, `pacman -Sy`, `pacman -S charly`, assert the packaged `/usr/bin/charly` version equals the version `pacman -Q` recorded (`ARCH-REPO-VERSION-OK`), assert the welded command plugins dispatch project-less from `/tmp` (`ARCH-WELDED-CMD-OK`), remove `charly` and install `charly-minimal`, and assert the minimal plugin set (`ARCH-MINIMAL-PLUGIN-SET-OK`).
- `.gitignore` (new, +16) — the generated-artifact ignores: `.build/`, `.containerignore`, `.dockerignore`, `.check/`, `output/`, editor scratch (`*.swp`, `*~`), migration rollback backups (`*.bak`, `*.bak.*`).

No Go, no Containerfile, no workflow, no image build — additive declarative config only. Nothing in the diff builds an image; the VM template is a plain `cloud_image`.

**Title alignment:** the PR title is `feat: add the check-arch-repo R10 bed for the published package repo` — aligned with the diff's bed id `check-arch-repo` (previously the title said `check-charly-arch-repo`, a stale name; corrected).

## Change class

**candy/box-config** (a new project config plus a disposable VM check bed; no image build, no Go).

**Closes / relates to:** relates to `opencharly/charly#625` — the companion PR that removes these same entities (`check-arch-repo` + `arch-repo-vm`) from `opencharly/charly`'s `charly.yml`. The two PRs must land together; until #625 merges, the bed has two homes, which is the R3 duplication this relocation eliminates.

**Breaking change + rollback:** none — additive config in a repo that previously declared none. Rollback is a revert of this PR (together with the companion removal in `opencharly/charly#625`).

## How R10-tested

- **Gate for this change class:** `charly check run check-arch-repo` (the full R10 sequence: build → check image → deploy → check live → fresh `update` rebuild → check live again → teardown).
- **Bed / substrate / result:**

| bed | substrate | ok | steps | seconds |
|---|---|---|---|---|
| `check-arch-repo` | vm (libvirt, `cloud_image` Arch) | true | 10 | 434 |

### The three version-like numbers — explicitly distinguished (B12/A2 reconciliation)

The previous review round flagged the pasted run's stamp `2026.266.1123` as unreconcilable with the body's version claims. The three numbers are **three different things** and are not expected to be equal:

| # | Number | What it is | Where it comes from |
|---|---|---|---|
| 1 | `2026.249.2125` | **charly binary version** (`charly version`) — the build identity of the worktree dev binary that produced the run | `charly version` (compile-time build stamp) |
| 2 | `2026.249.2125` | **project schema/stamp** — the `version:` field in this PR's `charly.yml` (line 13) | the diff (`charly.yml:13`) |
| 3 | `2026.266.1123` | **run CalVer** — a wall-clock RUN TIMESTAMP minted at invocation, naming the `.check/check-arch-repo/<calver>/` artifact directory | `time.Now().UTC()` in `plugin-check/candy/plugin-check/calver.go` |

Numbers 1 and 2 are equal by design (R9: binary == source). Number 3 is a timestamp and is **never** the binary version nor the project stamp — it is "what time is it now" when the run was minted. The cited source says so verbatim:

```
$ grep -n -e 'func ComputeCalVer' -e 'NEVER the identity' -e 'time.Now' -e 'what time is it' \
    plugin-check/candy/plugin-check/calver.go
25:// is chronological. This is "what time is it now" (used to TAG an artifact created
26:// at this moment) — NEVER the identity of the charly binary.
27:func ComputeCalVer() string {
28:	return ComputeCalVerAt(time.Now().UTC())
32:func ComputeCalVerAt(t time.Time) string {
```

The run CalVer `2026.266.1123` decodes to day-of-year 266 of 2026 (2026-09-23) at 11:23 UTC — the moment the bed was invoked. The binary version `2026.249.2125` decodes to day 249 (2026-09-06), when that dev binary was built. A binary built on day 249 and run on day 266 is exactly the expected relationship; the two are independent axes, so their inequality is not a contradiction. **The run CalVer never equals the binary version or the project stamp, and must not be conflated with either.**

- **Binary:** `charly version` → `2026.249.2125` (worktree dev build) — the binary version (number 1 above), matching the `version:` stamp in the diff (number 2 above). This is independent of the run CalVer `2026.266.1123` (number 3).
- **Fresh clone at this tree — anchored to the TREE, not a mutable tip SHA (A1):** `git clone https://github.com/opencharly/charly-arch` → `/tmp/opencode/rem-carch-7`. The R10 evidence below was captured at head `16640f4`. Every commit from `16640f4` to the current PR head is either a merge or a body-only `docs: re-freeze the PR body` EMPTY commit, so all of them resolve to the SAME tree `cf73e72b9a5e39748d746c6f70508157f7490501`. The evidence is therefore keyed to the tree hash (which cannot go stale across re-freeze commits), not to any particular tip SHA — the current head, whatever it is, is tree-identical to the evidence head:

```
$ cd /tmp/opencode/rem-carch-7 && git rev-parse 'HEAD^{tree}' '16640f4^{tree}'
cf73e72b9a5e39748d746c6f70508157f7490501
cf73e72b9a5e39748d746c6f70508157f7490501
$ git diff origin/main...HEAD --stat
 .gitignore |  16 +++++++
 charly.yml | 147 +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 163 insertions(+)
```

The re-freeze commit is created with `git commit --allow-empty` and adds zero files (its `git show --stat` lists no file lines), which is exactly why `HEAD^{tree}` stays `cf73e72…` across re-freezes — so the R10 evidence applies to the current head without re-capture.

- **Did the changed path run live?** yes — the bed this PR adds (`check-arch-repo`) executed end-to-end against the real published `opencharly.github.io/charly-arch` pacman repo, installing the real `charly` and `charly-minimal` packages inside the disposable Arch VM and running the fresh-rebuild leg.
- **Concurrency:** N/A — a single bed on a single `ssh.port_auto` VM; there are no parallel legs to schedule.
- **Zero warnings:** the run's `summary.yml` reports `errors: 0`, `warnings: 0`, `advisories: 0`; the 11 `severity-prefix` findings are the allowlisted `dev-worktree-binary-notice` (the dev binary deliberately skips the installed plugin set and says so once per process).

*1. Real gate output — `charly check run check-arch-repo` (full, unfiltered):*

```
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/charly-arch=/tmp/opencode/rem-carch-7" \
    charly check run check-arch-repo
Destroyed VM charly-check-arch-repo
charly check run check-arch-repo: [vm-build] START (charly vm build; log: .check/check-arch-repo/2026.266.1123/vm-build.log)
charly check run check-arch-repo: [vm-build] PASS after 3.599s [allowlisted=1] (log: .check/check-arch-repo/2026.266.1123/vm-build.log)
charly check run check-arch-repo: [vm-create] START (charly vm create; log: .check/check-arch-repo/2026.266.1123/vm-create.log)
charly check run check-arch-repo: [vm-create] PASS after 1.236s [allowlisted=1] (log: .check/check-arch-repo/2026.266.1123/vm-create.log)
charly check run check-arch-repo: [deploy-add] START (charly deploy add; log: .check/check-arch-repo/2026.266.1123/deploy-add.log)
charly check run check-arch-repo: [deploy-add] PASS after 9.895s [allowlisted=1] (log: .check/check-arch-repo/2026.266.1123/deploy-add.log)
charly check run check-arch-repo: [bring-up-members] START
charly check run check-arch-repo: [bring-up-members] PASS after 2ms
charly check run check-arch-repo: [check-live] START (charly check live; log: .check/check-arch-repo/2026.266.1123/check-live.log)
charly check run check-arch-repo: [check-live] PASS after 2m40.736s [allowlisted=1] (log: .check/check-arch-repo/2026.266.1123/check-live.log)
charly check run check-arch-repo: [update] START (charly update; log: .check/check-arch-repo/2026.266.1123/update.log)
charly check run check-arch-repo: [update] PASS after 1m19.409s [allowlisted=5] (log: .check/check-arch-repo/2026.266.1123/update.log)
charly check run check-arch-repo: [rebuild-ready] START
charly check run check-arch-repo: [rebuild-ready] PASS after 270ms
charly check run check-arch-repo: [check-live-rebuild] START (charly check live; log: .check/check-arch-repo/2026.266.1123/check-live-rebuild.log)
charly check run check-arch-repo: [check-live-rebuild] PASS after 2m51.654s [allowlisted=1] (log: .check/check-arch-repo/2026.266.1123/check-live-rebuild.log)
charly check run check-arch-repo: [cleanup] START (charly vm destroy; log: .check/check-arch-repo/2026.266.1123/cleanup.log)
charly check run check-arch-repo: [cleanup] PASS after 7.15s [allowlisted=1] (log: .check/check-arch-repo/2026.266.1123/cleanup.log)
charly check run check-arch-repo: [cleanup-members] START
charly check run check-arch-repo: [cleanup-members] PASS after 0s
charly check run check-arch-repo: PASS (steps=10)

$ tail -n 8 .check/check-arch-repo/2026.266.1123/summary.yml
  - name: cleanup-members
    duration_seconds: 0
    ok: true
total_seconds: 434
diagnostics:
  errors: 0
  warnings: 0
  advisories: 0
  allowlisted: 11
```

*2. The changed path executing — the added bed's live steps (`check-live.log` and `check-live-rebuild.log`):*

```
VM: charly-check-arch-repo (ssh arch@127.0.0.1:38379)
  PASS  check the charly GPG key is imported and locally signed  [arch-repo-key-imported]  exit=0
  PASS  check the [charly] repo is added to pacman.conf  [arch-repo-added]  exit=0
  PASS  check pacman -Sy refreshes the charly repo db  [arch-pacman-sy]  exit=0
  PASS  check pacman -S charly installs the package  [arch-charly-installed]  exit=0
  PASS  check charly version equals the repo's published CalVer  [arch-charly-version]  exit=0
  PASS  check the welded command plugins dispatch project-less from /tmp  [arch-welded-command-project-less]  exit=0
  PASS  check charly-minimal installs after removing charly (both provide /usr/bin/charly)  [arch-charly-minimal-installed]  exit=0
  PASS  check the minimal variant ships only the doctor/clean/settings plugin set  [arch-minimal-plugin-set]  exit=0

8 steps: 8 passed, 0 failed, 0 skipped

VM: charly-check-arch-repo (ssh arch@127.0.0.1:34787)   # fresh-rebuild leg
  PASS  check the charly GPG key is imported and locally signed  [arch-repo-key-imported]  exit=0
  PASS  check the [charly] repo is added to pacman.conf  [arch-repo-added]  exit=0
  PASS  check pacman -Sy refreshes the charly repo db  [arch-pacman-sy]  exit=0
  PASS  check pacman -S charly installs the package  [arch-charly-installed]  exit=0
  PASS  check charly version equals the repo's published CalVer  [arch-charly-version]  exit=0
  PASS  check the welded command plugins dispatch project-less from /tmp  [arch-welded-command-project-less]  exit=0
  PASS  check charly-minimal installs after removing charly (both provide /usr/bin/charly)  [arch-charly-minimal-installed]  exit=0
  PASS  check the minimal variant ships only the doctor/clean/settings plugin set  [arch-minimal-plugin-set]  exit=0

8 steps: 8 passed, 0 failed, 0 skipped

$ charly version
2026.249.2125
$ charly box validate
charly box validate: OK — checked 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors
```

*3. Docs-only evidence:*

```
N/A — not a docs-only change.
```

## Attribution tier

**fully tested and validated** — the added bed's full R10 sequence (10/10 steps) executed live against this repo's checkout on a fresh rebuild, at 0 errors / 0 warnings / 0 advisories. Binary version `2026.249.2125` == project `version:` stamp `2026.249.2125`; the run CalVer `2026.266.1123` is the invocation timestamp and is not either of those.

## Harness rulebook compliance

- **R0 skills:** loaded `/charly-check:check` (bed model, R10, ADE), `/charly-image:image` + `/charly-image:layer` (box/candy authoring), `/charly-internals:git-workflow` (PR-only landing), and `/charly-internals:root-cause-analyzer` before acting.
- **R1 RCA + zero warnings:** every gate run reports 0 errors / 0 warnings / 0 advisories; the only findings are the allowlisted `dev-worktree-binary-notice` (the dev binary deliberately skips the installed plugin set). No warning is parked.
- **R2 no out-of-scope:** the change is confined to the two files that add the bed; the source-side removal is the NAMED companion `opencharly/charly#625`, not a forward-looking deferral.
- **R3 no duplication:** the bed has ONE canonical home after both PRs land — the repo that owns the artifact. Real sweep (bed present on `charly` main, removal staged in #625, addition in this PR):
```
$ grep -n -e '^arch-repo-vm:' -e '^check-arch-repo:' charly-main.yml        # opencharly/charly@main
288:arch-repo-vm:
1702:check-arch-repo:
$ grep -n -e '^-arch-repo-vm:' -e '^-check-arch-repo:' pr625.diff          # companion removal (#625)
53:-arch-repo-vm:
461:-check-arch-repo:
$ grep -n -e '^arch-repo-vm:' -e '^check-arch-repo:' charly-arch/charly.yml # this PR
27:arch-repo-vm:
47:check-arch-repo:
```
- **R4 no workaround:** no sleep/retry/shim/magic-number; the bed drives the real published repo and the real package manager.
- **R4a product-first:** no missing charly verb was routed around; the bed is authored declaratively in the owning repo.
- **R5 hard cutover:** the relocation IS the cutover — the `charly`-side entities (`check-arch-repo`, `arch-repo-vm`) are deleted in the same wave by `opencharly/charly#625`; no legacy alias or shadow copy is left behind.
- **R6 git safety:** feature branch off `origin/main`; no force-push, no history rewrite; caught up with `origin/main` by merge.
- **R7 runtime gate:** the end-to-end `charly check run check-arch-repo` gate ran (not a static check and not `charly check live`).
- **R8 artifact invariants:** `N/A — no Containerfile-generation or ai.opencharly.* label change.`
- **R9 binary == source:** the bed ran on the worktree dev binary `charly version 2026.249.2125`; the diff's `version:` stamp is `2026.249.2125`. The run CalVer `2026.266.1123` in the pasted `.check/...` paths is a **run timestamp minted at invocation** (`plugin-check/candy/plugin-check/calver.go`: `ComputeCalVer` returns `ComputeCalVerAt(time.Now().UTC())`, documented "what time is it now … NEVER the identity of the charly binary") — it is NOT the binary version and NOT the project stamp. The three values: binary `2026.249.2125`; project `charly.yml:version` `2026.249.2125`; run CalVer `2026.266.1123`.
- **R10 disposable + coverage:** proven on a `disposable: true` target only; the fresh rebuild (`update` + `check-live-rebuild`) passed; the added bed IS the coverage for the published-repo install path. The evidence was captured at head `16640f4`; the current PR head is tree-identical (`HEAD^{tree}` == `16640f4^{tree}` == `cf73e72b…`, since every later commit is an empty body re-freeze), so the evidence applies to the current head; the run CalVer `2026.266.1123` is the invocation timestamp, independent of the binary version `2026.249.2125` and the `version:` stamp.
- **RDD / ADE / SDD:** RDD — the bed proves the real published artifact at runtime; ADE — the bed carries a `description:` + deterministic `check:` steps and `charly box validate` reports 0 errors; SDD — `N/A — no .cue edit.`
- **Hard cutover:** one atomic config commit; the companion removal is the matching leg; no Phase-2/TODO in scope.
- **Kernel/plugin boundary:** `N/A — declarative config in the owning repo; no core/sdk code.`
- **Disposable-only autonomy:** every destroy/rebuild was on the `disposable: true` bed.
- **Clean architecture + Go gates:** `N/A — no Go changed.`
- **CHANGELOG:** `N/A — this repo's tag-on-merge writes CHANGELOG/<CalVer>.md from this PR body at merge.`

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*